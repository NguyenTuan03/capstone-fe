import { buildMetadata } from '@/@crema/helper/seo';

export const metadata = buildMetadata({
  title: 'Quản lý buổi học',
  description:
    'Quản lý lịch học, theo dõi buổi học và xử lý báo cáo từ học viên và huấn luyện viên',
  path: '/sessions',
});
