import { buildMetadata } from '@/@crema/helper/seo';

export const metadata = buildMetadata({
  title: 'Quản lý người dùng',
  description:
    'Quản lý tài khoản người dùng, huấn luyện viên và học viên trong hệ thống Pickleball',
  path: '/users',
});
