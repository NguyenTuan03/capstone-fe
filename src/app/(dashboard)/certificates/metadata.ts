import { buildMetadata } from '@/@crema/helper/seo';

export const metadata = buildMetadata({
  title: 'Xác minh chứng chỉ - Duyệt đơn đăng ký huấn luyện viên',
  description: 'Duyệt đơn đăng ký huấn luyện viên và xác minh chứng chỉ chuyên môn',
});
