import { buildMetadata } from '@/@crema/helper/seo';

export const metadata = buildMetadata({
  title: 'Xác minh huấn luyện viên',
  description: 'Xem xét và duyệt đơn đăng ký làm huấn luyện viên, quản lý hồ sơ và chứng chỉ',
  path: '/coaches',
});
