import CurriculumPageClient from '@/modules/dashboard/curriculum';
import { generateMetadata } from './metadata';

export { generateMetadata };

export default function CurriculumPage() {
  return <CurriculumPageClient />;
}
